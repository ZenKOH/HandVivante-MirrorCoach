function renderExercises() {
  const family = $('exerciseFamilyFilter')?.value || 'all'; const level = $('exerciseLevelFilter')?.value || 'all';
  const filtered = EXERCISES.filter(x => (family === 'all' || x.family === family) && (level === 'all' || x.levels.includes(level)));
  $('exerciseGrid').innerHTML = filtered.length ? filtered.map(x => `<article class="exercise-card"><img src="${E(x.image)}" alt=""><div class="exercise-body"><div class="tags"><span class="tag">${E(x.family)}</span>${x.levels.map(l => `<span class="tag">${E(l)}</span>`).join('')}</div><h3>${E(x.title)}</h3><p>${E(x.purpose)}</p><button type="button" class="secondary exercise-details" data-id="${E(x.id)}">View clinical details</button></div></article>`).join('') : '<div class="empty">No exercises match the current filters.</div>';
}

function openExercise(id) {
  const x = EXERCISES.find(e => e.id === id); if (!x) return;
  $('exerciseDialogContent').innerHTML = `<p class="eyebrow">${E(x.family)}</p><h2>${E(x.title)}</h2><img src="${E(x.image)}" alt="" class="tutorial-visual"><div class="detail-list"><article><strong>Clinical purpose</strong><p>${E(x.purpose)}</p></article><article><strong>Setup</strong><p>${E(x.setup)}</p></article><article><strong>Movement pattern</strong><p>${E(x.pattern)}</p></article><article><strong>Starting dose</strong><p>${E(x.dose)}</p></article><article><strong>Record</strong><p>${E(x.measure)}</p></article><article><strong>Watch for</strong><p>${E(x.compensation)}</p></article><article><strong>Progression</strong><p>${E(x.progression)}</p></article></div>`;
  $('exerciseDialog').showModal();
}

function renderTutorialSteps() {
  const completed = new Set(state.tutorial.completed || []);
  $('tutorialSteps').innerHTML = TUTORIAL_STEPS.map((s, i) => `<button type="button" class="tutorial-step ${i === tutorialIndex ? 'active' : ''} ${completed.has(i) ? 'done' : ''}" data-tutorial="${i}">${i + 1}. ${E(s.title)}</button>`).join('');
  const percent = Math.round(completed.size / TUTORIAL_STEPS.length * 100);
  $('tutorialProgressText').textContent = `${percent}% complete`;
  $('tutorialProgressBar').style.width = `${percent}%`;
}

function tutorialContent(step) {
  switch (step.type) {
    case 'overview': return `<p class="eyebrow">Module 1</p><h2>Meet HandVivante™ MirrorHand</h2><p>HandVivante is a bilateral robotic hand system designed to translate movement of a guiding hand into movement of an affected hand. The supplied brochure highlights task-oriented bimanual training, bilateral movement, and passive range-of-motion practice.</p><img src="assets/product-hero.svg" alt="HandVivante system" class="tutorial-visual"><div class="cue-box"><strong>MirrorCoach’s role</strong><p>This application organises screening, tutorial, protocol prescription, session scripting, functional exercises, outcome tracking, and research documentation. This public build does not control the hardware.</p></div>`;
    case 'components': return `<p class="eyebrow">Module 2</p><h2>Know the three core components</h2><div class="component-grid"><article class="component-card"><img src="assets/robotic-hand.svg" alt="Robotic hand"><h3>Robotic hand</h3><p>Actuator-driven exoskeleton fitted to the affected hand.</p></article><article class="component-card"><img src="assets/sensor-glove.svg" alt="Sensor glove"><h3>Sensor glove</h3><p>Detects individual finger movement of the guiding hand.</p></article><article class="component-card"><img src="assets/control-box.svg" alt="Control unit"><h3>Control unit</h3><p>Supports the available device modes. Verify version-specific controls in the IFU.</p></article></div><div class="manual-warning"><strong>Before every use:</strong> inspect all components, straps, interfaces, cables, connectors, and the workspace.</div>`;
    case 'mechanism': return `<p class="eyebrow">Module 3</p><h2>How Robotic Mirror Hand Therapy closes the loop</h2><p>Conventional mirror therapy provides a strong visual signal. RMHT adds matching movement-related sensory input from the affected hand and explicit attention to the intended movement.</p><div class="mechanism-diagram"><div class="mechanism-node"><strong>Mirror visual feedback</strong><span>The patient sees an apparently normal affected-hand movement.</span></div><div class="mechanism-arrow">→</div><div class="mechanism-node"><strong>Synchronous robotic movement</strong><span>The affected hand physically moves at the same time.</span></div><div class="mechanism-arrow">→</div><div class="mechanism-node"><strong>Intent + attention</strong><span>The patient imagines and attempts the movement.</span></div></div><div class="cue-box"><strong>Lay explanation</strong><p>The brain sees the hand move and feels the affected hand move at the same moment, while the patient actively thinks about and attempts the movement.</p></div>`;
    case 'selection': return `<p class="eyebrow">Module 4</p><h2>Select the right patient</h2><p>The supplied training material lists MAS &lt;3, stable physiological status, MMSE ≥24, and Brunnstrom arm stage ≥2 as screening conditions.</p><div class="detail-list"><article><strong>Check the person</strong><p>Diagnosis, time since onset, cognition/communication, pain, tone, passive range, skin, sensation, vision/attention, goals, and tolerance.</p></article><article><strong>Check the limb</strong><p>No unsafe fracture, tendon injury, fixed contracture, skin disorder, or other condition that cannot tolerate external movement.</p></article><article><strong>Check the pathway</strong><p>Severe: passive-follow with motor attempt. Moderate: active-assist. Mild: reduced assistance and dexterity focus.</p></article></div><div class="stop-box"><strong>Never automate eligibility</strong><p>MirrorCoach can structure the screen, but the treating clinician confirms suitability and parameters.</p></div>`;
    case 'setup': return `<p class="eyebrow">Module 5</p><h2>Position and fit the system</h2><img src="assets/task-bimanual.svg" alt="Bimanual HandVivante setup" class="tutorial-visual"><ol><li>Seat the patient upright with feet and forearms supported.</li><li>Place the mirror at midline and hide the affected hand from direct view.</li><li>Fit the sensor glove to the guiding hand.</li><li>Support the affected limb and align the robotic hand without forcing range.</li><li>Check thumb position, straps, skin, sensation, wrist alignment, and cables.</li><li>Identify the IFU-specified pause/stop process before movement.</li></ol>`;
    case 'synchrony': return `<p class="eyebrow">Module 6</p><h2>Verify movement synchrony</h2><p>Begin with a slow, limited-range open-close check. Observe both hands, the patient’s face and posture, and the device.</p><div class="phase-checks"><label class="check-row"><input type="checkbox"><span>Affected hand follows the intended direction smoothly.</span></label><label class="check-row"><input type="checkbox"><span>No painful end range, pressure, obstruction, or sustained tone response.</span></label><label class="check-row"><input type="checkbox"><span>No unexpected delay, motion, sound, heat, or warning indicator.</span></label><label class="check-row"><input type="checkbox"><span>Patient understands how to signal discomfort and pause.</span></label></div><div class="stop-box"><strong>Do not “work through” desynchronisation.</strong><p>Pause, support the limb, assess the fit and device, and follow the current IFU and local escalation process.</p></div>`;
    case 'session': return `<p class="eyebrow">Module 7</p><h2>Deliver the standard 30-minute session</h2><ol class="architecture-list"><li><b>3 min</b><span>Safety & readiness</span></li><li><b>4 min</b><span>Calibration & familiarisation</span></li><li><b>5 min</b><span>Priming with mirror gaze, imagery and attempted movement</span></li><li><b>15 min</b><span>Two high-repetition movement families</span></li><li><b>3 min</b><span>Doffing, functional transfer and cooldown</span></li></ol><div class="cue-box"><strong>Therapist script</strong><p>“Watch the reflected hand. Imagine it is your affected hand. Begin your effort just before the robot moves. Feel the fingers moving at the same time as the movement you see.”</p></div><p>Record delivered minutes, cycles, range, speed, assistance, mirror gaze, pain, skin, interruptions, functional transfer and any protocol deviation.</p>`;
    default: return `<p class="eyebrow">Module 8</p><h2>Safety and competency check</h2><p>Select one answer for each question, then score the module.</p><form id="tutorialQuiz" class="quiz"><article class="quiz-question"><strong>1. When may high-repetition practice begin?</strong><label class="check-row"><input type="radio" name="q1" value="a"><span>Immediately after power-on</span></label><label class="check-row"><input type="radio" name="q1" value="b"><span>After fit, comfort, low-range movement and synchrony are verified</span></label></article><article class="quiz-question"><strong>2. Which is an immediate stop reason?</strong><label class="check-row"><input type="radio" name="q2" value="a"><span>New numbness, skin change, escalating pain or unexpected movement</span></label><label class="check-row"><input type="radio" name="q2" value="b"><span>The patient completes fewer repetitions than planned</span></label></article><article class="quiz-question"><strong>3. What is the purpose of functional transfer?</strong><label class="check-row"><input type="radio" name="q3" value="a"><span>To prove the robotic dose equals independent recovery</span></label><label class="check-row"><input type="radio" name="q3" value="b"><span>To test whether the primed movement carries into unassisted real-world action</span></label></article><article class="quiz-question"><strong>4. Which document takes precedence?</strong><label class="check-row"><input type="radio" name="q4" value="a"><span>The current manufacturer IFU and local policy</span></label><label class="check-row"><input type="radio" name="q4" value="b"><span>This public prototype</span></label></article><button class="primary" type="submit">Score competency check</button></form><div id="quizResult"></div>`;
  }
}

function renderTutorial() {
  renderTutorialSteps();
  $('tutorialContent').innerHTML = tutorialContent(TUTORIAL_STEPS[tutorialIndex]);
  $('tutorialPrevBtn').disabled = tutorialIndex === 0;
  $('tutorialNextBtn').textContent = tutorialIndex === TUTORIAL_STEPS.length - 1 ? 'Score the competency check below' : 'Mark complete & next';
  $('tutorialNextBtn').disabled = tutorialIndex === TUTORIAL_STEPS.length - 1;
}

function completeTutorialStep() {
  if (tutorialIndex === TUTORIAL_STEPS.length - 1) return showStatus('Complete and pass the competency check to finish the tutorial.', 'error');
  const completed = new Set(state.tutorial.completed || []); completed.add(tutorialIndex); state.tutorial.completed = [...completed]; saveState();
  tutorialIndex++; renderTutorial();
}

function scoreQuiz(event) {
  event.preventDefault(); const form = new FormData(event.target); const answers = { q1: 'b', q2: 'a', q3: 'b', q4: 'a' };
  const score = Object.entries(answers).reduce((n, [q, a]) => n + (form.get(q) === a ? 1 : 0), 0);
  const passed = score === 4;
  $('quizResult').innerHTML = `<div class="quiz-result ${passed ? 'good' : 'warning'}"><strong>${score}/4 correct.</strong> ${passed ? 'Knowledge check passed in this prototype. Local supervised competency sign-off is still required.' : 'Review the tutorial and operating manual, then try again.'}</div>`;
  if (passed) { const completed = new Set(state.tutorial.completed || []); completed.add(TUTORIAL_STEPS.length - 1); state.tutorial.completed = [...completed]; saveState(); renderTutorialSteps(); }
}
